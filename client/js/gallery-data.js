/**
 * VANI KARAIKTA — ARTWORK CATALOG DATA
 * Structured catalog of artworks, series, mediums, and metadata.
 */

const GALLERY_DATA = [];

if (typeof module !== 'undefined' && module.exports) {
  module.exports = GALLERY_DATA;
}
